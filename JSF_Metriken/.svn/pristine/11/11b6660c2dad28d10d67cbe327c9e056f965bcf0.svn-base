package br.com.metriken.model.dao.base;

/**
 * Classe genérica que deve ser estendida por todas as classes DAO's. Aqui,
 * estão os métodos que existirão em todos os DAO's, além do EntityManage.
 * 
 * @author Matheus
 * 
 * @param <T>
 *            A Entidade que será manipulada.
 */
public class AbstractGenericDAO<T> implements GenericDAO<T> {

	/**
	 * 
	 */
	private static final long serialVersionUID = -1831396111962553214L;

	// @PersistenceContext
	// protected EntityManager entityManager;

	@Override
	public void persist(T object) {
		// this.entityManager.persist(object);
	}

	@Override
	public void remove(T object) {
		// this.entityManager.remove(object);
	}

	@Override
	public void update(T object) {
		// this.entityManager.merge(object);
	}

}
